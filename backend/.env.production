# Production Environment Variables - synix.fr
# Ce fichier sert de référence pour les variables en production
# Les valeurs réelles sont gérées par GitHub Secrets

NODE_ENV=production
PORT=3000

# MongoDB Atlas (même URI qu'en développement)
MONGODB_URI=mongodb+srv://evanbertin69110_db_user:fCmNMIWj0VJ3l89I@luzartworkbd.jgpze9j.mongodb.net/luzartwork?retryWrites=true&w=majority

# JWT Secret (DOIT être différent de dev en production !)
# Générer avec: openssl rand -base64 64
JWT_SECRET=REMPLACER_PAR_UN_SECRET_FORT_EN_PRODUCTION

# Frontend URL (domaine production)
FRONTEND_URL=https://synix.fr
